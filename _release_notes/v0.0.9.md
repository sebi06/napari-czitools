# napari-czitools v0.0.9

Release date: 2026-03-12

## Highlights

- Added compatibility with czitools v0.15.0 `read_stacks` return signature changes.
- Fixed sample-data loading paths that could fail after the upstream tuple-shape update.
- Preserved backward compatibility with older czitools `read_stacks` return values.

## What Changed

- Added compatibility handling for `read_stacks` return values in:
  - `src/napari_czitools/_io.py`
  - `src/napari_czitools/_reader.py`
- Updated compatibility documentation in:
  - `README.md`
  - `.github/copilot-instructions.md`

## Validation

- Full test suite run in the `smartmic` environment:
  - `python -m pytest -q`
  - Result: `134 passed, 16 warnings`

## Notes

- CI/PyPI publication is triggered from tags matching `v*`.
- This release is created from `main`.
